# 05 — Real planner integration (GPT-OSS-120B, structured output)

**What to build:** A real user concept produces real, schema-valid `HeliosParams` via GPT-OSS-120B, persisted as the pipeline's `modality: text` row. The image stage can remain stubbed at this point.

**Blocked by:** 01, 02, 03

**Status:** ready-for-agent

**Team:** Single-Agent Structure Team

Ordered by dependency. The first three unblock everything after them, so working top-to-bottom should not hit a wall.

**Cost is neurons-only in this ticket.** The `cost_usd` column holds the raw neuron count for now, not dollars; the rate var and the conversion are deliberately out of scope. Neurons are what the model reports and cannot be recovered after the fact, so capturing them is the part that has to happen now. Converting to dollars later is an in-place multiply over stored rows once the rate is settled.

- [ ] **`planConcept` gains `env` and `p_invoc_id`.** Its signature is `planConcept(concept: string)` today — it can reach neither the `AI` binding, nor `PLANNER_MODEL`, nor `AI_GATEWAY_ID`, nor the invocation id, and most boxes below need at least one of them. `p_invoc_id` is minted in `pipeline.ts:48` and never leaves it — **Arham Zahid** (owns `services/pipeline.ts`, makes the call)
- [ ] **`getTextualModelOutput` returns the usage data, not just the params.** It returns `z.infer<T>` and throws the raw response away, so `usage.neurons` and anything describing the model that actually ran is unrecoverable. The metadata and cost boxes below cannot be done until this is fixed — return something like `{ data, usage, model }`, or accept a callback. This is a `shared-utils` signature change, so weigh it against the six engines that will inherit it — **Arham Zahid** (owns the helper)
- [ ] Confirm the AI Gateway auth setup before the first live call. There is no `apps/agent-helios/.dev.vars` in the repo; if the `helios` gateway has authentication on, the token goes in `.dev.vars` locally and `wrangler secret put` for deploy, never in `wrangler.jsonc` `vars`. `.gitignore:163` already excludes `.dev.vars*` while allowing `.dev.vars.example`, and no example file exists — add one so the required keys are discoverable without asking — **Hashir Rauf** (LLM/AI integration)
- [ ] `tools.ts` imports `HeliosParamsSchema` from `@aureline/shared-types` and passes it straight to `getTextualModelOutput`, plus the thin call itself. **Do not define the schema here.** The original wording said `tools.ts` "contains the `HeliosParams` schema definition", which made sense when the helper took raw JSON Schema and someone had to hand-write one. It takes the Zod schema now and derives the JSON Schema itself, so a second copy would only drift — and `HeliosParamsSchema` is the contract `pipeline.ts`, `imageGenerator.ts` and both prompt files already run on — **Arham Zahid** (owns `tools.ts`)
- [ ] `services/planner.ts` calls GPT-OSS-120B **through AI Gateway** (ADR-0006), using structured output (JSON schema), not tool-calling. The Gateway is not a separate binding — it is the `gateway` option on `ai.run`'s third argument, on the same `AI` binding. `getTextualModelOutput` already supports it, but it is **opt-in**: `buildAiRunOptions` returns `undefined` when no id is passed (`aiGateway.ts:54`), so a forgotten `gateway` option quietly calls Workers AI directly instead of failing — no error, no log entry, no token counts — **Hashir Rauf**
- [ ] The model id comes from `env.PLANNER_MODEL`, not a string literal in `planner.ts`. It is already `@cf/openai/gpt-oss-120b` in `wrangler.jsonc:64`. Note `worker-configuration.d.ts:9` types it as that exact string *literal*, not `string`, so changing the model in `wrangler.jsonc` also means regenerating types or TypeScript will reject the new value — **Hashir Rauf**
- [ ] The planner call passes `{ gateway: { id: env.AI_GATEWAY_ID, metadata: { p_invoc_id } } }` to `getTextualModelOutput`, and after the first real call the request is visible in the AI Gateway dashboard log with token counts and cost (ADR-0006). `GatewayConfig.metadata` already supports this (`aiGateway.ts:30`); the missing piece is `p_invoc_id` reaching the planner, per the first box. `env.AI.aiGatewayLogId` is non-null after a routed call and is the cheapest way to confirm routing without opening the dashboard — **Hashir Rauf**
- [ ] `services/pipeline.ts`'s planner stage is swapped from the ticket-01 stub to this real implementation — **Arham Zahid** (owns `services/pipeline.ts`)
- [ ] No new inline DB code. Metadata and cost reach storage through `repository/do.repository.ts`'s existing functions; `pipeline.ts` stays free of Drizzle. Specifically `completeTextRun(db, pInvocId, params)` takes no cost or metadata today, so it needs new parameters — that extension is expected, a second write path is not. Ticket 03 skipped naming its target file and needed a follow-up refactor to undo it — **Arham Zahid**
- [ ] `TEXT_MODEL_METADATA_STUB` is retired. `pipeline.ts` hardcodes `{ model: "gpt-oss-120b", provider: "openai", temperature: 1 }`; real values must come from the call that actually ran, or the audit row records a model that was never used — **Arham Zahid**
- [ ] Token counts and the raw `neurons` count from `usage` are written into the text row's `model_metadata`, **and the neuron count also goes into the `cost_usd` column** so cost is queryable without unpacking JSON. It is neurons in that column for now, not dollars — the column is a `real` (`db/schema.ts:27`) so the number fits, but the name will not match the contents until the rate lands, so say so wherever it is read. Recording neurons is the part that cannot be reconstructed later; converting to dollars is a later in-place multiply. Note ticket 03 shipped without the `neurons: 0` placeholder it describes, so this adds the key rather than filling it in — **Arham Zahid**
- [ ] A real concept string produces schema-valid `HeliosParams`, persisted correctly as the `modality: text` row from ticket 03 — **Hashir Rauf** (review/verification)

**The `shared-utils` prerequisites are already done**, so this ticket starts from a working helper. `getTextualModelOutput` now sends the schema to the model as `text.format` rather than only checking the reply against it, opens the `gpt-oss-120b` `output` envelope before validating (a `reasoning` item precedes the `message` item), takes the system prompt via `options.instructions` separately from the user input, and defaults to 2 attempts to match `MAX_RETRIES`. 32 tests pass, up from 22.

Two things that follow from it, both easy to trip over:

- **`PLANNER_MODEL` cannot be pointed at Llama 3.3 70B or GLM 4.7 Flash.** They are named as fallbacks in `docs/Models Summary.md`, but they want the chat-completions request shape and only the Responses API shape is supported. The call fails at the provider, not anywhere legible. See `docs/adr/0007-responses-api-only-for-structured-output.md`.
- **Pass `maxRetries` explicitly** from config. The helper's default is a standalone-use fallback, not policy, and every attempt is a billed call.

**Cost warning:** every `wrangler dev` request against the `AI` binding bills real Workers AI quota, and the retry loop multiplies that by `maxRetries` per request. Build the unwrapping against a recorded or fake response first; only then point it at the live model.
